// export const loadLocalStorage = async (keys: string | string[]): Promise<{[key: string]: any}> => {
//   return new Promise(resolve => resolve(chrome.storage.sync.get(keys)))
// }

// export const setLocalStorage = async (items: {
//   [key: string]: any;
// }) => {
//   new Promise(resolve => {
//     resolve(chrome.storage.sync.set(items))
//   })
// }
